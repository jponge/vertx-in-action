name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  schedule:
    - cron: '0 9 * * *'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        java: [ 8, 11 ]
        os: [ ubuntu-latest, windows-latest ]
    steps:
    - uses: actions/checkout@v2
    - name: Setup java
      uses: actions/setup-java@v1
      with:
        java-version: ${{ matrix.java }}
    - name: Build the chapter-specific examples
      run: ./gradlew --no-daemon build-chapters
      shell: bash
    - name: Build the part2 10k steps challenge application
        run: ./gradlew --no-daemon build-part2-steps-challenge
        shell: bash
